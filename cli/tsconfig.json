{
  "//": "Type-check-only config for the standalone nexus CLI. cli/** is NOT in the main tsconfig (src/** only) and esbuild bundles without type-checking, so build-cli.mjs runs `tsc -p cli` to catch errors (e.g. a dropped import) that would otherwise ship silently.",
  "compilerOptions": {
    "noEmit": true,
    "strict": true,
    "skipLibCheck": true,
    "module": "ESNext",
    "target": "ES2020",
    "moduleResolution": "bundler",
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "types": ["node"]
  },
  "include": ["**/*.ts"]
}
