# `src/agents/agentManager/modes/batchExecutePrompt`

## Purpose
Submodule for "agentManager" agent implementation.

## What's Here
- Subfolders: `services`, `types`, `utils`
- Files: `BatchExecutePromptMode.ts`, `index.ts`

## Improvement Ideas
- Tighten mode parameter typing and reduce `any` at the tool boundary.
- See `CODEBASE_AUDIT.md` (repo root) for cross-cutting cleanup opportunities.
