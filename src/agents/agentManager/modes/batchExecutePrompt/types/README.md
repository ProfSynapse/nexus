# `src/agents/agentManager/modes/batchExecutePrompt/types`

## Purpose
Type definitions scoped to "agentManager" features.

## What's Here
- Subfolders: _None_
- Files: `BatchExecuteTypes.ts`, `ExecutionResult.ts`, `index.ts`

## Improvement Ideas
- Consider consolidating/re-exporting types to reduce cross-folder imports.
- Tighten mode parameter typing and reduce `any` at the tool boundary.
- See `CODEBASE_AUDIT.md` (repo root) for cross-cutting cleanup opportunities.
