# `src/agents/memoryManager/modes`

## Purpose
Mode implementations (individual tools) for agent "memoryManager".

## What's Here
- Subfolders: `sessions`, `states`, `utils`, `workspaces`
- Files: `index.ts`

## Improvement Ideas
- Tighten mode parameter typing and reduce `any` at the tool boundary.
- See `CODEBASE_AUDIT.md` (repo root) for cross-cutting cleanup opportunities.
