# `src/utils/schemas/builders`

## Purpose
Shared utilities (schema helpers, validation, context handling, misc helpers).

## What's Here
- Subfolders: _None_
- Files: `BatchExecuteSchemaBuilder.ts`, `ContentBatchSchemaBuilder.ts`, `ExecuteSchemaBuilder.ts`, `index.ts`, `SessionSchemaBuilder.ts`

## Improvement Ideas
- Prevent "utils" sprawl: promote stable helpers into well-named modules.
- See `CODEBASE_AUDIT.md` (repo root) for cross-cutting cleanup opportunities.
