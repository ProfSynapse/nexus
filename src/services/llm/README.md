# `src/services/llm`

## Purpose
LLM integration (provider adapters, streaming, validation, provider manager).

## What's Here
- Subfolders: `adapters`, `core`, `providers`, `streaming`, `types`, `utils`, `validation`
- Files: `ImageFileManager.ts`, `ImageGenerationService.ts`, `index.ts`, `LLMSettingsNotifier.ts`

## Improvement Ideas
- Add a short contract note for this module (inputs/outputs, side effects).
- See `CODEBASE_AUDIT.md` (repo root) for cross-cutting cleanup opportunities.
