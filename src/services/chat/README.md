# `src/services/chat`

## Purpose
Chat/conversation coordination services used by the native chat UI.

## What's Here
- Subfolders: `builders`
- Files: `ChatService.ts`, `ConversationContextBuilder.ts`, `ConversationManager.ts`, `ConversationQueryService.ts`, `CostTrackingService.ts`, `index.ts`, `StreamingResponseService.ts`, `ToolCallService.ts`

## Improvement Ideas
- Add a short contract note for this module (inputs/outputs, side effects).
- See `CODEBASE_AUDIT.md` (repo root) for cross-cutting cleanup opportunities.
