# `src/types`

## Purpose
Type definitions shared across the codebase.

## What's Here
- Subfolders: `agent`, `chat`, `common`, `llm`, `mcp`, `memory`, `migration`, `pagination`, `plugin`, `search`, `storage`
- Files: `google-genai.d.ts`, `index.ts`

## Improvement Ideas
- Consider consolidating/re-exporting types to reduce cross-folder imports.
- See `CODEBASE_AUDIT.md` (repo root) for cross-cutting cleanup opportunities.
